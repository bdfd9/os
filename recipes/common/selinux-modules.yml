# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: script
    scripts:
      - selinux/install-selinux-policies.sh
      - selinux/set-selinux-booleans.sh
