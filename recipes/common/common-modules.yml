# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: script
    scripts:
      - http-mirrors.sh
      # - install-pinned-kernel
      - common/kernel-packages.sh

  - type: files
    files:
      - source: system/base
        destination: /

  - from-file: common/common-packages.yml
  - from-file: common/common-scripts.yml
