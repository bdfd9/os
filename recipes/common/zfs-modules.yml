# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: script
    scripts:
      - install-zfs.sh
  - type: files
    files:
      - source: system/zfs
        destination: /
