# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: files
    files:
      - source: system/base/usr
        destination: /usr
      - source: system/base/etc
        destination: /etc

  - type: script
    scripts:
      - http-mirrors.sh
      - install-repos.sh
      - install-copr-repos.sh
      - install-pinned-kernel

  - from-file: common/common-packages.yml
